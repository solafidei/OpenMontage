# OpenMontage - Core Dependencies
pyyaml>=6.0
pydantic>=2.0
jsonschema>=4.20
python-dotenv>=1.0
Pillow>=10.0
numpy>=1.24
requests>=2.31
google-auth>=2.0       # service-account auth for Google Cloud TTS and google_imagen's Vertex AI route
google-genai>=1.0.0    # Gemini Developer API clients for Gemini image models, Lyria, Veo, and Gemini Omni
openai>=2.44.0         # Videos API support for Sora 2
librosa>=0.10.1        # beat/onset analysis — analyze-beatgrid.py, the beat_grid tool
soundfile>=0.12.1      # WAV decode for the beat analyser (librosa's audio backend)

# Backlot - the living storyboard (local board server)
fastapi>=0.110
uvicorn>=0.29
watchfiles>=0.21
