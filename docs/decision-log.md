# Decision Log

Every decision Claude brought to the user for a ruling, with the outcome.
Maintained by Claude: a new entry is appended every time the user is asked to
decide or approve something. Newest entries at the bottom.

| # | Date | Decision | Options considered | Ruling |
|---|------|----------|--------------------|--------|
| 1 | 2026-09-01 | **Fate of untracked styles/custom/** (`backward-pass.yaml`, `evidence-room.yaml`) — ruled during the [Epic-1 review-fixes](intent/epic1-review-fixes-spec.md) epic breakdown (H1) | gitignore as generated output of `playbook_generator.save_playbook()` *(chose)* vs commit the two playbooks as shared styles | **Gitignore `styles/custom/`**, alongside the other generated-output entries; the two existing playbooks stay local. Tracked as issue #30; loader tests must stay hermetic against locally-generated playbooks. Implemented — `styles/custom/` is ignored (.gitignore, alongside `projects/`), and G5's `tests/styles/test_playbook_loader.py` generates its own playbook in a tmp_path rather than depending on one on disk. |
| 2 | 2026-09-01 | **How the review-fix ledger rollout relates to issue #6**, which deliberately deferred screen-demo + beta pipelines | new issue referencing #6 under the review-fixes epic *(chose)* vs widening #6's scope in place | **New issue #32 (under epic #25) supersedes #6's deferral** — the shared checkpoint protocol now embeds cost snapshots in every pipeline, so unwired pipelines checkpoint false $0 for paid runs; #6 itself stays untouched for epic #1's bookkeeping. |
| 3 | 2026-09-01 | **Status for a render whose audio was never measured** — E1 (issue #29) stopped the narration gate asserting an absence it never measured, but an indeterminate verdict carries no critical keyword, so a failed/timed-out probe returned `status: "pass"` / `present_to_user`. `main` returns `revise`/`re_render` for the same input. | keep the spec's choice (its Risk section accepts the detection regression by name) vs promote `indeterminate` to critical (fails closed, but contradicts E1's wording constraint and its acceptance test 2) vs **a distinct `needs_verification` status** *(chose)* | **Add `needs_verification` / `human_review`.** Keeps E1's honesty fix — never assert what was not measured — without letting an unverified render read as a clean pass. Deliberate, owner-approved deviation from E1's wording constraint; the spec's acceptance test 2 (`status != "revise"`) still passes unchanged. Enums in `schemas/artifacts/final_review.schema.json` extended; `skills/meta/reviewer.md` already escalates any action that is not `present_to_user`. |
| 4 | 2026-09-01 | **Breadth of A1's legacy `cost_snapshot` tolerance** (issue #26). The specified helper keys on `exc.field != "cost_snapshot"`, and `exc.field` derives from `absolute_path[0]`, so it swallows every error under the subtree — `cost_snapshot="n/a"`, `None` and `{"total_spent_usd": "lots"}` all read clean and advance stages, though the docstring claims it tolerates "exactly the legacy shape". | keep as specced (a narrower tolerance risks re-breaking legacy resume — the deadlock A1 exists to fix) vs **narrow to the legacy shape** *(chose)* vs narrow it and also harden the consumers | **Narrow to the legacy shape**: an object whose `*_usd` values are real numbers, carrying keys the tightened schema no longer allows. Non-dict `cost_snapshot` and non-numeric money now fail loudly again on all three validated read/write paths. **Scope correction:** this does NOT protect `backlot/state.py` — it globs `checkpoint_*.json` and does a raw `json.load` (`backlot/state.py:45`), never calling `read_checkpoint`, so the tolerance helper is bypassed and a bare string or non-numeric money still reaches the cost meter at `:618`. `reconstruct_from_snapshot` is covered only when its snapshot arrives via a validated read; it remains a public staticmethod that accepts any dict. Consumer hardening was offered and not chosen; tracked as a follow-up. The single-definition property across all three call sites is preserved, and `test_legacy_project_resumes_end_to_end` stays green as the guard against over-narrowing. |
| 5 | 2026-09-01 | **Commit granularity for Epic 25** (issue #25, 64 files, +3725/−252 across 7 workstreams) | one commit for the epic vs **one commit per issue** *(chose)* vs leave uncommitted for review | **Nine commits mapping to #26–#34**, so each closes its own issue and the epic bisects per workstream. |
| 6 | 2026-09-03 | **Cutaway cap for [reel-batch](intent/reel-batch-spec.md)** (issue #46) — the spec's $0.50 five-reel worst case is not reachable under greedy allocation: 5 cuts a reel against a 20-segment pool fills four reels outright, giving 4 reels at $0.00, not 5 at $0.50 | no cap, fill every short slot (a thin pool bills up to $2.50 and a reel could be mostly generated) vs drop the reel count until the pool covers it outright (paid valve becomes dead code) vs **at most one AI flash cut per reel** *(chose)* | **One AI flash per reel.** `assisted_ceiling = usable_segments // (cuts_per_reel - 1)`; `cutaways = max(0, reels * cuts - have)`. Reproduces the spec's arithmetic exactly (25 → $0.00, 20 → 5 reels/$0.50, 19 → 4 reels/$0.10), matches R8's "sub-second flash accents" reading, and caps a bad batch at $0.50. The cap is a new ruling, not in the spec — `idea-director.md` carries the worked table. |
| 7 | 2026-09-03 | **How many human gates [reel-batch](intent/reel-batch-spec.md) has** (issue #46) — the issue's table describes two, but `pipeline_defs/reel-batch.yaml` sets `human_approval_default: true` on `publish` as well, and `lib/checkpoint.py` enforces the manifest value | two gates, dropping `publish` to `false` (batch runs unattended to a finished export package) vs **three gates** *(chose)* | **Three gates: `idea`, `scene_plan`, `publish`.** Gate 2 stays the last one before any paid call, which is what licenses unattended `assets`; gate 3 puts per-reel caption copy and posting order in front of the operator before packaging rather than at posting time. `executive-producer.md` documents three. |
| 8 | 2026-09-04 | **Full-run pre-authorisation for the `gym-reels-w36` sitting** — [reel-batch](../pipeline_defs/reel-batch.yaml) gates `idea`, `scene_plan` and `publish`, and the operator asked for a full end-to-end test in one message | stop at each of the three gates *(the manifest default)* vs **run through, presenting each gate's artifacts** *(chose)* | **Ran through.** Operator's words: "you can go ahead and do the remaining open items ... use that for the full test". Recorded as an `approval_policy` entry in the run's own `decision_log` per [AGENT_GUIDE.md](../AGENT_GUIDE.md) — which required adding `approval_policy` to `decision_log.schema.json`, since the guide named a category the schema rejected. What was waived is the wait, not the review; every gate's numbers were presented. Nothing in the sitting spent money. |
| 9 | 2026-09-04 | **One track, five reels** — `executive-producer.md` says `tracks: [] # one per reel, never shared`, and the operator supplied a single track (Iniko — Jericho, 186s) for a five-reel batch. `script-director.md` requires escalating this rather than solving it quietly | **5 reels from 5 non-overlapping windows of the one track** *(chose)* vs add 4 more tracks vs 1 reel from the 1 track vs generate 4 tracks via `music_generation` | **Amended the rule for this sitting, knowingly.** Each reel takes a distinct beat-anchored window (69.9–144.7s across the track) with its own hook, its own grid offset and its own captions. The rule's stated harm — "the batch reads as one video cut into pieces" — is real and accepted; mitigated by a one-reel-a-day posting slot rather than pretending it away. Recorded on `script.metadata.single_track_amendment` and in `publish_log.metadata.single_track_note`. Five reels shipped at $0.00. |
| 10 | 2026-09-04 | **Whether estimate/execute divergence reaches the cost ledger** (issue #51's open criterion) — #40 offered "refused, or recorded on the ledger entry"; the implementation stamps `ToolResult.data`, which `cost_tracker` never reads | route it onto the ledger entry vs **warn into the run log and document the gap** *(chose)* vs refuse pre-execution | **Documented as out of scope, with the reason, and a warning added so it is not invisible.** Verified: `cost_tracker` exposes no public mutator for an existing entry, `video_selector.execute` is handed no `cost_entry_id`, and `grep` finds *no production code that constructs a `CostTracker` at all* — the ledger is driven entirely by the stage-director markdown fences. Closing it properly means changing how directors book spend, which is a product decision, not a defect fix. A later adversarial pass then found the warning itself bypassable four ways and crash-prone on an unpriced quote; all five are fixed in `275cf29`. |
| 11 | 2026-09-04 | **What a persistent footage record is for** — [`footage_library`](../tools/video/footage_library.py) wrote its corpus to `projects/<batch>/corpus`, so every sitting re-indexed an unchanged pool. Two readings of "store a record for each video": save the reprocessing, or also remember which clips a past batch already used | processing cost only *(chose)* vs also tracking cross-batch clip reuse (corpus grows a used-in history, `clip_ledger` moves out of the project) | **Processing cost only.** Operator: "yes processing cost", then confirmed reuse across batches "is fine" — i.e. acceptable, not requested. Scope held to caching; `assert_no_reuse()` stays a within-batch guarantee. Noted at the time: with 33 usable segments against 25 per batch, a cross-batch no-reuse rule would have blocked a second batch until more footage arrived. Shipped in `cb4033c` + `63b4ef0`; cold 1171.8s → warm 75.1s on the operator's own 67-file pool, suite 2412 green |
| 12 | 2026-09-08 | **Whether caption motion is a third `preset` value or its own axis** — `preset` fused motion (`popScale`), typography (`strokeRatio`, `uppercase`) and layout (`wordGapRatio`, `safeZone`) into one enum, plus the pill decision one file up, so `reel_pop`'s type treatment could not be had without its motion and no value expressed "hold still" | a third `preset` value `reel_pop_static` (multiplies out — every future typography preset needs a static twin) vs three inputs for motion/typography/layout (layout is already independently settable via `safe_zone`) vs **a separate `animation_preset` axis** *(chose)* | **`animation_preset: {none, fade, pop}`, with NO default.** Absent means "whatever `preset` implies", emits no props key, and keeps every existing render byte-identical for `talking-head` and `avatar-spokesperson`. The `preset -> motion` table lives in TypeScript only; a Python copy would put one table in two languages with nothing pinning them equal. `fade` and `pop` are renames of shipped behaviour, only `none` is new code. Motion carries its own `minWordGapRatio` floor because `reel_pop`'s `0.4` gap was tuned to survive the pop, not chosen typographically — splitting them made `{default, pop}` reachable for the first time. Shipped `ff04355`, `a7d1801`, `874dc6d`; spec at [animation-preset-spec.md](intent/animation-preset-spec.md). |
| 13 | 2026-09-08 | **How many members `caption_source` ships with** — research proposed `{speech_stem, video_sound, music_bed, none}`; the tree supports one. Operator was asked directly whether he would ever post a captionless reel and answered "not sure, build it anyway", then ruled again once the cost was known | build all four as specified vs build `music_bed` + `none` (the operator's first answer) vs **ship `music_bed` alone, with the other four refused and given written re-entry conditions** *(chose)* vs skip the field entirely | **One member: `music_bed`.** `speech_stem` has no stem separation anywhere in the tree; `video_sound` has no routing path (`footage_library.py:323` passes `transcribe: False` for the whole pool, and `transcriber` is granted to `script`, which never sees the pool); `voice_over` has no producer, every track being music. `none` was refused on evidence the operator did not have when first asked: `hook` is required on every `reel_plan` entry and its text is rebased off transcribed words, so a captionless reel has no hook copy either and needs an operator-authoring stage the parent spec already deferred — plus four other consumers. Re-presented with that cost, the operator chose the one-member enum. **What it buys, stated honestly: no per-reel information today.** It closes the vocabulary, makes the four refusals machine-checkable rather than prose, and is the widening point when a producer appears. An enum member with no producer would be accepted by `validate_artifact` and refused only by a reviewer — a guard bypassed by omission. Spec at [caption-source-spec.md](intent/caption-source-spec.md). |
| 14 | 2026-09-08 | **How a shortfall slot's unused time is filled** — the branch review found `asset-director.md` hard-coding `flash_seconds: 0.6` for a slot `scene-director` had sized as a full beat-grid interval (~1.94s), so a reel with a cutaway rendered ~1.3s short and every later cut drifted off the beat, with no gate able to catch it ([branch-review-fixes-spec.md](intent/branch-review-fixes-spec.md) W6.2). The first implementation resolved it by sizing the flash to the slot (`min(slot, 2.0)`), which contradicts R8 | **cutaway stays sub-second, the adjacent operator slot extends to absorb the remainder** *(chose)* vs let the cutaway fill its slot up to 2.0s (overturns R8) vs plan fewer cuts per reel when the pool is short (reel cuts slower than approved) vs revert to the hardcoded 0.6 and leave the drift defect open | **Sub-second flash; the neighbour absorbs.** R8 ([reel-batch-spec.md](intent/reel-batch-spec.md):687) resolved cutaway length as "sub-second flash accents, not B-roll", and §335 names the harm the rejected option would cause: "in a 10s reel a 2s cutaway is 20% of the runtime spent not showing the thing the viewer came for". So `scene-director` shrinks a shortfall slot to the flash length and hands the remainder to an adjacent operator slot; reel duration and the beat grid are both preserved, and the AI stays an accent rather than becoming b-roll. **Known consequence, accepted:** the absorbing neighbour's leading edge is then no longer a grid line, so G3's "every cut slot snapped to a beat from its own reel's grid" gains an explicit exception for that one edge, and `scene-director`'s step-2 "slot edges are grid lines" is qualified the same way. On a reel's LAST slot the flash goes at the tail instead, which costs the flash its own beat — the honest trade, since the alternative moves the reel's approved end. Distinct from ruling #6, which capped the cutaway COUNT (one AI flash per reel) and said nothing about length. |
| 15 | 2026-09-09 | **Commit granularity for the fal.ai / Gemini model-knowledge refresh** — an audit of every provider fact in the tree (85 files: tool code, tests, docs, and both Layer 2 director and Layer 3 provider skills) moved three defaults and corrected a price the tool had been under-quoting ~6x. The code moves and the doc corrections assert the same facts, so ordering them matters | one commit for the whole refresh *(chose)* vs one commit per layer (tools / tests / docs / skills) vs the three default moves first and the doc corrections after | **One commit.** Operator's reason: "a split commit leaves the tree self-contradicting in between" — any intermediate commit would have `kling_video.py` pricing a 5s cutaway at one number while `cutaway_gen.py`, the reel-batch scripts and `PROVIDERS.md` still stated another, and a bisect landing there would read as a real defect. **What moved.** Defaults: `imagen-4.0-generate-001` -> `gemini-3.1-flash-image` (the legacy Imagen ids stay in the enum, they are still public); `gemini-omni-flash-preview` -> `gemini-omni-1.1-flash` and the fal endpoints to `google/gemini-omni-flash/v1.1/*` (forced, not elective — Google shuts the preview down 2026-09-30); `lyria-3-pro-preview` -> `lyria-3.5` at the same flat $0.08, with the 5s/184s duration bounds relabelled from model limits to **tool** floor/ceiling because 3.5 publishes neither. **The Kling finding.** `kling_video.py` never sent `generate_audio` and `fal_schemas.json` declares it `default: true` on every `v3/*` endpoint, so the pinned cutaway route has been billing fal's audio-on rate of $0.126/s — $0.63 for the 5s clip the tool quoted at a flat `$0.10`. The estimate was not stale by a rounding error but by ~6x, and `v2.1/master` by ~4.7x ($0.30 quoted, $1.40 billed). `estimate_cost` now reads a real per-second price table and `execute` sends `generate_audio` explicitly, so the bill equals the estimate instead of inheriting a server-side default the tool never saw. Ruled with three independent adjudicators returning 3-0 on the $0.63 reading, each proving it from shipped behaviour rather than preference. **Also note:** this is the one and only edit the audit made to this append-only log — entry #14's line pointer, `reel-batch-spec.md:666` -> `:687`, which the reel-batch edits had made stale. |
| 16 | 2026-09-11 | **What the pinned cutaway route pays for audio** — the model-knowledge refresh (#15) taught `kling_video.estimate_cost` to read fal's real per-second table, which exposed that `cutaway_gen._provider_inputs` passed no `generate_audio` at all and so inherited fal's audio-on default: $0.126/s, $0.63 for the pinned 5s `v3/standard` clip. That sits *above* `single_action_approval_usd: 0.50`, and a five-reel shortfall at $3.15 overruns the `budget_default_usd: 2.00` the manifest derives — while under R8 a cutaway is a ~0.6s silent flash accent cut into a music-driven reel, so the generated audio is discarded before it ever reaches the edit | **pin `generate_audio: false` and keep the budget** *(chose)* vs keep fal's audio-on default and raise `budget_default_usd` to ~$3.50 to clear the 10% reserve vs leave the tool alone and re-derive only the prose | **Audio off.** Operator's reason: the edit throws the audio away, so audio-on was paying 50% more for nothing. `_provider_inputs` now sets `generate_audio: False` explicitly, which makes fal's $0.084/s audio-off rate the one that applies: **$0.42 per pinned clip, $2.10 for a five-reel shortfall**. Three consequences resolve in the same direction. `budget_default_usd: 2.00` and the manifest's `5 x $0.42 = $2.10` derivation are correct as written and did not move — the comment now states *why* $0.42 is the real price rather than leaving it as an unexplained coincidence. §5.4's approval argument stands rather than inverting: $0.42 < $0.50, so the single-action threshold still does not fire on a per-reel entry, and `user_approved=True` stays the belt-and-braces contract note it always was. And the estimate/execute divergence figures re-derive against $0.42 — an unpinned reroute bills seedance's $1.52 against the $0.42 the guards approved (**~3.6x**, not the ~15x the pre-fix docs claimed nor the ~2.4x an audio-on reading gives), and a five-clip batch bills $7.60 against one $0.42 quote (**~18x**, not 76x). **Both prices stay live:** a generic `kling_video` quote that passes no `generate_audio` still inherits audio-on at $0.63/clip; only the cutaway pin is $0.42, and every doc anchor now says which of the two cases it means. **Verification.** The refresh's 85 files were re-reviewed by two independent adversarial passes (138 and 188 agents, 8 lenses); 103 raw findings narrowed to **77 that survived 3-refuter verify** (38 + 39, deduped across the two sets), and all 77 were applied and then re-verified in fresh context before this commit. Four were code defects rather than prose: the `generate_audio` idempotency-key omission, the unguarded `v2.1` duration/operation combinations `kling_video`'s widened schema admits, `recraft_image`'s hex->RGBColor conversion sitting outside its `try:` (an uncaught `ValueError` on 3-digit shorthand), and `lyria-recipe.py`'s `--scale` default having been promoted from a silently-ignored placeholder to a hard key constraint. |
